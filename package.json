{
  "name": "chen-editor",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --test dist/test/**/*.test.js",
    "pretest": "npm run build",
    "cli": "node dist/src/cli.js",
    "render:example": "node dist/src/cli.js examples/music.chen --out examples/music.svg"
  },
  "dependencies": {
    "commander": "^12.1.0",
    "svgdom": "^0.1.19"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.7.0"
  }
}
